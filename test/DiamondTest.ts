'use strict';

import { expect } from 'chai';

describe("Diamond Kata", () => {

    beforeEach(() => {

    });

    it("Should be true ...", () => {
        expect(true).to.be.eq(true);
    })

});
